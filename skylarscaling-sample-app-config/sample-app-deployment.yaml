---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: skylarscaling-sample-app
  namespace: skylarscaling-sample-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: skylarscaling-sample-app
  template:
    metadata:
      labels:
        app: skylarscaling-sample-app
    spec:
      containers:
        - name: skylarscaling-sample-app
          image: "quay.io/sscaling/gitops-workshop:v0.0.42"
          command:
            - '/usr/bin/greet'
          args:
            - 'Hello skylarscaling\!'
...
